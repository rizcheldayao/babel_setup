{
  "name": "babeltest",
  "version": "1.0.0",
  "description": "Babel set up",
  "author": "Rizchel Dayao",
  "license": "ISC",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d public"
  },
  "devDependencies": {
    "babel-cli": "^6.0.0",
    "babel-preset-es2015": "^6.18.0"
  }
}
